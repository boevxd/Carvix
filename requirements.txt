# Carvix AutoManager - Requirements
# Установка: pip install -r requirements.txt

# Обязательные зависимости
PyQt6>=6.0.0
requests>=2.28.0
google-auth-oauthlib>=1.0.0

# API сервер (синхронизация с веб-интерфейсом)
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
python-multipart>=0.0.9
pydantic>=2.0.0

# Опциональные зависимости (для графиков и экспорта)
matplotlib>=3.5.0
pandas>=1.3.0
reportlab>=3.6.0
